# version3.21 提交说明

## 1. 本次提交定位
本次提交属于“SQL 数据填充与可维护性优化”阶段，可直接用于当前 Demo/联调。

本次以以下文件为唯一数据源：
- `database/sql/version3.21/english_reading_listening_speaking.sql`

## 2. 本次已完成内容
- 完成 articles 相关数据填充与结构性校对。
- 完成 article_segments 样例翻译填充（用于演示与联调）。
- 补充关键维护注释（articles / article_segments / article_tags / users）。
- 优化 articles 分组标题：移除无意义编号（如 2.1/2.2），避免歧义。
- 调整维护规则位置：`articles` 规则前置到分组标题之前，避免被误解为仅约束某一个学科。

## 3. 当前导入方式（本阶段约定）
使用 SQL 文件直接导入：

```powershell
mysql -u root -p < "database/sql/version3.21/english_reading_listening_speaking.sql"
```

说明：
- 该 SQL 含 `DROP SCHEMA` 与 `CREATE SCHEMA`，会重建 `english_reading` 数据库。
- 导入前请确认本地数据已备份。

## 4. 与 migrations/seeders 的关系
当前阶段允许先提交 SQL 成果用于展示和联调。

但按项目规范，后续仍需完成：
- 迁移结构对齐到 `database/migrations/`
- 测试数据对齐到 `database/seeders/`

建议后续由数据库负责人统一收口，避免“SQL 导入”和“migrate --seed”两套流程长期并存。

## 5. 建议提交信息（可复制）
```text
feat(db): complete version3.21 SQL data fill and maintenance-comment cleanup

- finalize article/segment demo data for v3.21
- optimize section headers (remove redundant numeric prefixes)
- move global article maintenance rules to neutral position
- keep SQL import as current demo initialization path
```
